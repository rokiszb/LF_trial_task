<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Weekly Top 10 News Statistics</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.5; color: #333; }
        .container { max-width: 800px; margin: 0 auto; }
        h1 { color: #2c3e50; }
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 8px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background-color: #f2f2f2; }
        .footer { margin-top: 30px; font-size: 12px; color: #777; }
    </style>
</head>
<body>
<div class="container">
    <h1>Weekly Top 10 News Statistics</h1>
    <p>Here are the top 10 most viewed news articles from {{ date_range.start }} to {{ date_range.end }}:</p>

    <table>
        <thead>
        <tr>
            <th>#</th>
            <th>Title</th>
            <th>Views</th>
            <th>Published Date</th>
        </tr>
        </thead>
        <tbody>
        <tr>
        {% for news in top_news %}
                <td>{{ loop.index }}</td>
                <td>{{ news.title }}</td>
                <td>{{ news.commentCount }}</td>
                <td>{{ news.insertDate|date('Y-m-d') }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">No statistics available for this period</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        <p>This is an automated message. Please do not reply to this email.</p>
    </div>
</div>
</body>
</html>
